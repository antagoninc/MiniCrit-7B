apiVersion: v1
kind: Secret
metadata:
  name: minicrit-secrets
  namespace: minicrit
  labels:
    app.kubernetes.io/name: minicrit
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Placeholder - replace with actual secrets in overlays or external secret management
  HUGGINGFACE_TOKEN: "replace-with-actual-token"
  API_KEY: "replace-with-actual-api-key"
